/**
 * Linear word combination strategy (word1 + word2)
 */
export class LinearRecombination {
  combine(words: string[]): string[] {
    // TODO: Generate linear combinations
    // Example: ["cloud", "sync"] => ["cloudsync", "synccloud"]
    return [];
  }
}
